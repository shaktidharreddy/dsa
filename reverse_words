
def reverseWords(self, s: str) -> str:
        rev = ""
        for i in s.split(" ")[::-1]:
            if i!="":
                rev+= i + " "
        return rev.strip()

s = input("enter list of strings : ")
